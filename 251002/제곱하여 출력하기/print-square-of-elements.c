#include <stdio.h>

int main() {
    // Please write your code here.
    int n,a;
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d ",&a);
        printf("%d ",a*a);
    }
    return 0;
}